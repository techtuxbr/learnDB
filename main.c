#include <stdio.h>
#include <stdlib.h>
#include "file/file.h"

int main(){
    char token[255];

    readLDB("ufersa.ldb");

    /* 
    Enqueue("CRIAR"); Print(); 
    Enqueue("BANCO"); Print();
	Enqueue("\"ufersa\""); Print();
	Dequeue(token);  Print();;
    printf("\nToken que eu peguei: %s\n",&token);
	Enqueue(";"); Print();
    return 0;*/
}